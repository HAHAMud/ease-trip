export enum RoutePath {
  HOME = '/',
  PLANNING = '/planning',
  LOGIN = '/login',
  REGISTER = '/register',
  NOT_FOUND = '/404',
  ERROR = '/500'
}

export enum HeaderKey {
  AUTHORIZATION = 'Authorization'
}